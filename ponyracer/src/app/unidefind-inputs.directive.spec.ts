import { UnidefindInputsDirective } from './unidefind-inputs.directive';

describe('UnidefindInputsDirective', () => {
  it('should create an instance', () => {
    const directive = new UnidefindInputsDirective();
    expect(directive).toBeTruthy();
  });
});
